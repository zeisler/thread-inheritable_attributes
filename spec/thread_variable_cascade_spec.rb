require 'spec_helper'

describe ThreadVariableCascade do
  it 'has a version number' do
    expect(ThreadVariableCascade::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
